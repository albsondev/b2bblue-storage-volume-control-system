// Exemplo de funções para simular o comportamento do backend

export const generateCollectRequest = (stationId: number) => {
    console.log(`Pedido de coleta gerado para a estação ${stationId}`);
    // Implementar lógica para gerar pedido de coleta (simulado)
  };
  
  export const confirmCollect = (stationId: number) => {
    console.log(`Coleta confirmada para a estação ${stationId}`);
    // Implementar lógica para confirmar coleta (simulado)
  };
  